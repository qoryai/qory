apiVersion: qory.dev/v1alpha1
harness:
  target:
    runtime: claude
  modules:
    - name: core
      source: {path: modules/core}
      only:
        skills: [deploy]
  bind:
    agents/coder: core-coder
