apiVersion: qory.dev/v1alpha1
harness:
  target:
    runtime: claude
  modules:
    - name: ops
      source: {path: modules/ops}
      only:
        skills: [deploy]
      exclude:
        agents: [reviewer]
    - name: team
      source: {path: modules/team}
