apiVersion: qory.ai/v1alpha1
target:
  runtime: claude
modules:
  - name: ops
    source: {path: modules/ops}
    only:
      skills: [deploy]
    exclude:
      agents: [reviewer]
  - name: team
    source: {path: modules/team}
