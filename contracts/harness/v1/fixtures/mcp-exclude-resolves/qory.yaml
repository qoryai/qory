apiVersion: qory.dev/v1alpha1
harness:
  target:
    runtime: claude
  modules:
    - name: core
      source: {path: modules/core}
      exclude:
        mcp: [db]
    - name: nextjs
      source: {path: modules/nextjs}
