apiVersion: qory.ai/v1alpha1
kind: HarnessProfile
target:
  runtime: claude
  model: opus
layers:
  - name: core
    source: {path: layers/core}
    exclude:
      skills: [test]
      agents: [reviewer]
  - name: team
    source: {path: layers/team}
